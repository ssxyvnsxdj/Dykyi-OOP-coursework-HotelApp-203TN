<Window x:Class="HotelApp.RoomDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Details" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Бронювання" FontSize="20" Margin="10"/>
            <TextBlock Text="ПІБ бронювальника:" Margin="10"/>
            <TextBox Name="GuestName" Margin="10"/>
            <TextBlock Text="Дата заселення:" Margin="10"/>
            <DatePicker Name="CheckInDatePicker" Margin="10" SelectedDateChanged="CheckInDatePicker_SelectedDateChanged"/>
            <TextBlock Text="Дата виселення:" Margin="10"/>
            <DatePicker Name="CheckOutDatePicker" Margin="10" SelectedDateChanged="CheckOutDatePicker_SelectedDateChanged"/>
            <TextBlock Name="PriceTextBlock" Margin="10"/>
            <Button Content="Зберегти" Margin="10" Click="Save_Click"/>
            <Button Content="Відміна" Margin="10" Click="Cancel_Click"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Список бронювань" FontSize="20" Margin="10"/>
            <ListView Name="ReservationsList" Margin="10" SelectionChanged="ReservationsList_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding GuestName}" Width="250"/>
                            <TextBlock Text="{Binding CheckInDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                            <TextBlock Text="{Binding CheckOutDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                            <Button Content="Видалити" Click="DeleteReservation_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Window>